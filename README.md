# Pixel Porter - 图片处理管理工具

一个基于 Electron + React + TypeScript 开发的桌面端图片处理管理工具。

## 功能特性

- 📝 **批量文件重命名**：根据 EXIF 信息、文件创建时间或原始文件名批量重命名图片
- 🎨 **添加水印**：根据 EXIF 信息自动生成水印版本图片
- 📋 **EXIF 信息复制**：在图片之间复制 EXIF 信息

## 技术栈

- **桌面框架**: Electron
- **前端框架**: React 18 + TypeScript
- **UI 组件库**: Ant Design
- **状态管理**: Zustand
- **构建工具**: Vite
- **图片处理**: Sharp
- **EXIF 处理**: exifr

## 开发

### 前置要求

- Node.js >= 18.0.0
- pnpm >= 8.0.0

> **注意**: 本项目使用 pnpm 作为包管理器。如果尚未安装 pnpm，可以通过以下命令安装：
>
> ```bash
> npm install -g pnpm
> ```
>
> 或使用其他方式：https://pnpm.io/installation

### 安装依赖

```bash
pnpm install
```

使用 pnpm 安装后, 需要运行这个命令来编译 electron

```bash
pnpm approve-builds
```

### 开发模式

```bash
pnpm run electron:dev
```

这将同时启动 Vite 开发服务器和 Electron 应用。

### 构建

```bash
# 构建渲染进程
pnpm run build

# 构建 Electron 应用
pnpm run build:electron

# 打包应用
pnpm run electron:build
```

## 项目结构

```
pixel-porter/
├── src/
│   ├── main/              # Electron 主进程
│   ├── renderer/          # React 渲染进程
│   ├── preload/           # Preload 脚本
│   ├── shared/            # 共享代码
│   └── core/              # 核心业务逻辑
├── public/                # 静态资源
├── docs/                  # 文档
└── dist/                  # 构建输出
```

## 许可证

MIT
